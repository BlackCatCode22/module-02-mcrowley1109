hrs = input("Enter Hours: ")
h = float(hrs)
rt = input("Enter Rate: ")
pay = float(rt)
if h > 40:
    reg = h * pay
    otp = (h - 40.0) * (pay * 0.5)
    xp = reg + otp
else:
    xp = h * pay
print(xp)
